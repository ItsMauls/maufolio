import { Banner } from "./Banner"
import { Profile } from "./Profile"

export const Header = () => {
    return (
        <header>
            <Banner />
            <Profile />
        </header>
    )
}